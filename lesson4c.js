const add=function(num1,num2){
     let sum=num1+num2
     console.log(`The sum is ,${sum}`);
     
}
add(12,5)
const subtract=function(num1,num2){
     let subtraction=num1-num2
     console.log(`The answer is ,${subtraction}`);
     
}
subtract(12,5)

const division=function(num1,num2){
     let division=num1/num2
     console.log(`The division is ,${division}`);
     
}
division(12,6)
const modulus=function(num1,num2){
     let modulus=num1%num2
     console.log(`The modulus is ,${modulus}`);
     
}
modulus(47,5)




const area=function(base,height){
     let area_of_triangle=base*height/2
     console.log(`The area is ${area_of_triangle}`);
     
     
}
area(10,5)

const bmi=function(weight,height){
     let Bmi=weight/(height*height)
     console.log(`The bmi is ${Bmi}`);
     
}

bmi(76,1.93)

const Perimeter_of_a_circle=function(pie,Diameter){
     let perimeter=pie*Diameter
     console.log(`The perimeter is ${perimeter}`);
     
}
Perimeter_of_a_circle(3.142,7)

const si=function(principal,rate,time){
     let SI=principal*rate/100*time
     console.log(`The SI is ${SI}`);
     

}
si(30000,30,2)

const area_of_a_circle=function(pie,radius){
     let area=pie*radius**2
     console.log(`The area of the circle is ${area}`);
     
}
area_of_a_circle(3.142,7)

const area_of_a_triangle=function(base,height){
     let area=0.5*base*height
     console.log(`The area of the triangle is ${area}`);
     
}
area_of_a_triangle(6,7)

const area_of_a_square=function(side){
     let area=side*side
     console.log(`The area of the square is ${area}`);
     
}
area_of_a_square(7)